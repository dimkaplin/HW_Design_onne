<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hw_design_onne">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HW_Design_onne">
        <activity android:name=".ui.main.ActivityAnimationsExplodeRecycleViewItem"></activity>
        <activity
            android:name=".ActivityLessonFive"
            android:label="@string/title_activity_lesson_five"
            android:theme="@style/Theme.HW_Design_onne.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MAPIActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>